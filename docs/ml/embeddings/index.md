# Embeddings

Token IDs are integers — they carry no meaning on their own. Embeddings map each ID to a learned vector in a continuous space, where semantically similar tokens end up close together.

## Token Embeddings

## Positional Encodings

### Sinusoidal (original Transformer)

### RoPE (Rotary Position Embedding)

### ALiBi

*Coming soon.*
