# Vision Transformers (ViT)

Instead of convolutions, ViT splits an image into fixed-size patches, linearly embeds each patch, and feeds the sequence into a standard transformer encoder.

## Patch Embeddings

## Class Token

## Position Embeddings

## Comparison to CNNs

ViT requires more data than CNNs to train from scratch but scales better — making it the preferred backbone for large vision models and VLMs.

*Coming soon.*
