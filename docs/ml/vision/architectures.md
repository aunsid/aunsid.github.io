# CNN Architectures

## VGG

## ResNet and Residual Connections

The key insight: instead of learning a mapping H(x), learn the residual F(x) = H(x) - x. Makes training very deep networks feasible.

## EfficientNet

*Coming soon.*
