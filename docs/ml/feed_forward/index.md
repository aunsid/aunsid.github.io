# Feed-Forward Networks

Each transformer block contains a two-layer feed-forward network applied independently to each token position.

## Standard FFN

## Activation Functions (ReLU, GELU)

## Gated Variants (SwiGLU, GeGLU)

Used in LLaMA, PaLM, and most modern LLMs. Gated FFNs outperform standard FFNs for the same parameter count.

*Coming soon.*
